import WaterGrid from './WaterGrid';
import Bodies from '../body/Bodies';

describe('WaterGrid', () => {
    it('should do this', () => {
        const grid = new WaterGrid({ width: 10, height: 10 });
        grid.step
    });
    // describe('test cases', () => {
        // it
    // });
});
